/** @type {import('next').NextConfig} */
const nextConfig = {
  basePath: '/klagan',
  reactStrictMode: false,
  images: {
    domains: [
      "i.annihil.us"
    ]
  }
};

export default nextConfig;
